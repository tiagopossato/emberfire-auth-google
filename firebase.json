{
  "database": {
    "rules": "database.rules.json"
  },
  "hosting": {
    "firebase": "emberfire-auth-google",
    "public": "dist",
    "rewrites": [{
      "source": "**",
      "destination": "/index.html"
    }]
  }
}
